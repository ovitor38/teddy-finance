model User {
  id             String   @id @default(uuid())
  email          String   @unique
  passwordHashed String
  name           String?
  urls           Url[]
  createdAt      DateTime @default(now())
  updatedAt      DateTime
}
