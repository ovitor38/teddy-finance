model User {
  id        String   @id @default(uuid())
  email     String   @unique
  passwordHashed  String
  name      String?
  tasks     Task[]
  createdAt DateTime @default(now())
  updatedAt DateTime
}